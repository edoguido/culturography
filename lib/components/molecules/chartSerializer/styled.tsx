import styled from 'styled-components'

export const ChartSerializerContent = styled.div`
  position: relative;
  min-height: 320px;

  > * {
    position: absolute;
    inset: 0;
    z-index: 0;
  }
`

export const ChartSerializerWrapper = styled.div``

export default ChartSerializerWrapper
